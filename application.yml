spring:
  sql:
    init:
      mode: always
  datasource:
    url: aws-0-eu-central-1.pooler.supabase.com
    username: postgres.oxpaomatnfwsaodzyefz
    password: Diucamica23!
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
